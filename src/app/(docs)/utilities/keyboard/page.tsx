// Dual-URL alias: the canonical page lives at /keyboard; this mounts the same
// page under the Utilities group so /utilities/keyboard also works.
export { default, metadata } from '../../keyboard/page'
