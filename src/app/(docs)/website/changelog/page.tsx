// Dual-URL alias: the canonical page lives at /changelog; this mounts the same
// page under the Website group so /website/changelog also works.
export { default, metadata } from '../../changelog/page'
