// Dual-URL alias: the canonical page lives at /tree-of-life; this mounts the
// same page under the Devices group so /devices/tree-of-life also works.
export { default, metadata } from '../../tree-of-life/page'
