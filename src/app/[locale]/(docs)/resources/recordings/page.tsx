// Dual-URL alias: the canonical page lives at /recordings; this mounts the same
// landing under the Resources group so /resources/recordings also works.
export { default, generateMetadata } from '../../recordings/page'
