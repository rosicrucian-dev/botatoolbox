// Dual-URL alias: the canonical page lives at /cube-of-space; this mounts the
// same page under the Devices group so /devices/cube-of-space also works.
export { default, generateMetadata } from '../../cube-of-space/page'
