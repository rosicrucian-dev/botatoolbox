// Dual-URL alias: the canonical page lives at /timer; this mounts the same
// page under the Utilities group so /utilities/timer also works.
export { default, generateMetadata } from '../../timer/page'
