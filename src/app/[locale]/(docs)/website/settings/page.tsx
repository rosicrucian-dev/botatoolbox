// Dual-URL alias: the canonical page lives at /settings; this mounts the same
// page under the Website group so /website/settings also works.
export { default, generateMetadata } from '../../settings/page'
